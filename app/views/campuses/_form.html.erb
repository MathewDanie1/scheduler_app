<%= form_for(@campus) do |f| %>
  
  <div class="container">
    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :address %></br>
      <%= f.text_area :address %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
 <% end %>
</div>